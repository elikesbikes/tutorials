stages:
  - detect
  - deploy

detect_project:
  stage: detect
  image: alpine:latest
  before_script:
    - apk add --no-cache git
  script:
    - PROJECT=$(git diff --name-only HEAD~1 HEAD | grep "^docker-compose/" | cut -d/ -f2 | sort -u | head -1)
    - echo "Detected project: ${PROJECT:-none}"
    - echo "PROJECT_NAME=${PROJECT:-none}" >> deploy.env
  artifacts:
    reports:
      dotenv: deploy.env

deploy:ranger0:
  stage: deploy
  when: manual
  needs:
    - job: detect_project
      artifacts: true
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client rsync
    - chmod 600 "$SSH_PRIVATE_KEY"
  script:
    - |
      if [ "$PROJECT_NAME" = "none" ] || [ -z "$PROJECT_NAME" ]; then
        echo "No docker-compose project changed — nothing to deploy"
        exit 0
      fi
      echo "Deploying $PROJECT_NAME to ranger0 (192.168.5.16)..."
      ssh -i "$SSH_PRIVATE_KEY" -o StrictHostKeyChecking=no ecloaiza@192.168.5.16 "
        cd ~/devops/github/tutorials && git fetch origin && git reset --hard origin/main
        mkdir -p ~/devops/docker/${PROJECT_NAME}
        rsync -am \
          --include='*/' \
          --include='*.yml' \
          --include='*.py' \
          --include='*.sh' \
          --include='*.md' \
          --include='*.json' \
          --exclude='*' \
          ~/devops/github/tutorials/docker-compose/${PROJECT_NAME}/ \
          ~/devops/docker/${PROJECT_NAME}/
        cd ~/devops/docker/${PROJECT_NAME} && docker compose up -d
      "

deploy:endurance:
  stage: deploy
  when: manual
  needs:
    - job: detect_project
      artifacts: true
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client rsync
    - chmod 600 "$SSH_PRIVATE_KEY"
  script:
    - |
      if [ "$PROJECT_NAME" = "none" ] || [ -z "$PROJECT_NAME" ]; then
        echo "No docker-compose project changed — nothing to deploy"
        exit 0
      fi
      echo "Deploying $PROJECT_NAME to endurance (192.168.5.30)..."
      ssh -i "$SSH_PRIVATE_KEY" -o StrictHostKeyChecking=no ecloaiza@192.168.5.30 "
        cd ~/devops/github/tutorials && git fetch origin && git reset --hard origin/main
        mkdir -p ~/devops/docker/${PROJECT_NAME}
        rsync -am \
          --include='*/' \
          --include='*.yml' \
          --include='*.py' \
          --include='*.sh' \
          --include='*.md' \
          --include='*.json' \
          --exclude='*' \
          ~/devops/github/tutorials/docker-compose/${PROJECT_NAME}/ \
          ~/devops/docker/${PROJECT_NAME}/
        cd ~/devops/docker/${PROJECT_NAME} && docker compose up -d
      "
